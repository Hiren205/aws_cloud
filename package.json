{
  "name": "my-express-lambda",
  "version": "1.0.0",
  "scripts": {
    "build": "esbuild lambda.js --bundle --platform=node --target=node20 --outfile=dist/index.js"
  },
  "dependencies": {
    "@vendia/serverless-express": "^4.10.4",
    "express": "^4.18.2",
    "pg": "^8.20.0"
  },
  "devDependencies": {
    "esbuild": "^0.20.0"
  }
}
